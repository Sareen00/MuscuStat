{% extends 'template.html.twig' %}

{% block title %}Hello AdminController!{% endblock %}

{% block pageTitle %}Page Administrateur{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    .button { display: block; margin: 1em 0; padding: 1em; text-align: center; background-color: #003366; color: white; text-decoration: none; border-radius: 5px; width: 100%; }
    .button-container { display: flex; justify-content: space-between; }
    .button-column { width: 48%; }
</style>

<div class="example-wrapper">

    <div class="button-container">
        <div class="button-column">
            <a href="{{ path('app_gestion_utilisateur') }}" class="button">Utilisateurs</a>
            <a href="{{ path('app_exercice') }}" class="button">Exercices</a>
            <a href="#" class="button">Séances</a>
        </div>
        <div class="button-column">
            <a href="#" class="button">Page 1</a>
            <a href="#" class="button">Page 2</a>
            <a href="#" class="button">Page 3</a>
        </div>
    </div>
</div>
{% endblock %}
